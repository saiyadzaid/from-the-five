export const GET_CART = (status, payload) => ({
  type: "GET_CART",
  status,
  payload,
});
export const ADD_TO_CART = (status, payload) => ({
  type: "ADD_TO_CART",
  status,
  payload,
});
export const REMOVE_FROM_CART = (status, payload) => ({
  type: "REMOVE_FROM_CART",
  status,
  payload,
});