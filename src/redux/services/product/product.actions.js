export const GET_PRODUCTS = (status, payload) => ({
  type: "GET_PRODUCTS",
  status,
  payload,
});
export const GET_PRODUCT = (status, payload) => ({
  type: "GET_PRODUCT",
  status,
  payload,
});
export const SET_FILTERS = (status, payload) => ({
  type: "SET_FILTERS",
  status,
  payload,
});
